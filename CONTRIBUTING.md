# 贡献指南

感谢你愿意改进科研阅读工作台。项目追求本地优先、数据安全、阅读流畅和低门槛运行。

## 开始之前

- 搜索现有 Issue，确认问题尚未被报告。
- Bug 请提供最小复现步骤、浏览器与操作系统版本；不要上传私人论文、API Key、聊天记录或浏览器数据库。
- 较大的功能建议先开 Issue 说明使用场景、交互方案及对本地数据格式的影响。
- 安全漏洞不要提交公开 Issue，请遵循 [SECURITY.md](SECURITY.md)。

## 本地开发

仓库没有 npm 构建步骤。安装 Python 3.9+ 和 Node.js 后：

```bash
python tools/serve.py
python tools/check.py
```

浏览器使用 `http://localhost:8642/`。测试时建议换一个端口和独立浏览器配置，避免修改日常 IndexedDB 数据。

## 修改原则

- 保持纯静态、本地优先；新增运行依赖必须固定版本、保留许可证和来源完整性。
- PDF 渲染必须遵守画布像素、可见页和并发预算，离屏及时释放资源。
- 异步任务必须绑定论文 ID 与生命周期，切换后不得把旧结果写入新论文。
- IndexedDB 多表修改使用事务；保存失败必须保留编辑状态并向用户提示。
- 所有进入 `innerHTML` 的远端或用户内容都必须转义或经过 DOMPurify 清洗。
- 修改 JS/CSS 后同步更新 `index.html` 的资源版本参数。
- 新交互要验证 320px 窄屏、键盘可用性和 `prefers-reduced-motion`。

## 提交 Pull Request

1. 从 `main` 创建清晰命名的分支。
2. 保持提交聚焦，并说明问题、改动后的行为和验证方式。
3. 运行 `python tools/check.py`。
4. 涉及界面时附上无敏感内容的前后截图。
5. 涉及 PDF、保存或 AI 流式请求时，说明所用测试夹具与隔离方式。

提交贡献即表示你同意按项目的 MIT License 发布该贡献。
